import React from 'react';

export default class Artists extends React.Component {
    render (): React.ReactNode {
        return (
            <div>
                Artists Component
            </div>
        );
    }
}
